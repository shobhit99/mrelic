[SERVICE]
    Flush        1
    Daemon       Off
    Log_Level    info
    Grace        5
    Parsers_File /fluent-configs/parsers.conf
    Scripts_File /fluent-configs/format_json.lua

[INPUT]
    Name         stdin
    Tag          console.log

[FILTER]
    Name         lua
    Match        *
    Script       /fluent-configs/format_json.lua
    Call         format_record

[OUTPUT]
    Name         stdout
    Match        *
    Format       json_lines

[OUTPUT]
    Name         http
    Match        *
    Host         localhost
    Port         5959
    URI          /api/otel
    Format       json
    Header       Content-Type application/json
    Header       service.name payments-service
    Tls          off
    Tls.Verify   off
    log_response_payload true 